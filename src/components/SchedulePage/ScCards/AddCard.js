import React from 'react';

const AddCard = () => {
  return (
    <div className="add-card">
      <h4>Add a card...</h4>
      <button type="button">
        <i className="fas fa-plus-circle"></i>
      </button>
    </div>
  );
};

export default AddCard;
